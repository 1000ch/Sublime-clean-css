{
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "clean-css": "^5.3.2",
    "get-stdin": "^9.0.0"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  }
}
