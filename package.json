{
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "clean-css": "^5.3.0",
    "get-stdin": "^9.0.0"
  },
  "engines": {
    "node": ">=16",
    "npm": ">=7"
  }
}
