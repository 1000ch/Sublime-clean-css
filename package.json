{
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "clean-css": "^5.1.5",
    "get-stdin": "^9.0.0"
  },
  "engines": {
    "node": ">=12.20",
    "npm": ">=7"
  }
}
