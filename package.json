{
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "clean-css": "^5.2.2",
    "get-stdin": "^9.0.0"
  },
  "engines": {
    "node": ">=16",
    "npm": ">=7"
  }
}
